export interface ChatProps {
  // Add any props that Chat component might need here
  // For example:
  // messages: string[];
  // onSendMessage: (message: string) => void;
  // username: string;
  // channelId: number;
}

export interface Message {
  text: string
  name : string
}
