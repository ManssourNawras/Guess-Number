export interface RankingProps {
  // Add any props that Ranking component might need here
  // For example:
  // items: string[];
  // initialRank: number;
}

export interface PlayerRank {
  id : number
  name : string
  score : number
}
