export interface GameBoardProps {
  // Add any props that GameBoard component might need here
  // For example:
  // boardSize: number;
  // cellSize: number;
  // gameData: any[];
  // onCellClick: (row: number, col: number) => void;
}
