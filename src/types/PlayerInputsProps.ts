export interface PlayerInputsProps {
  // Add any props that PlayerInputs component might need here
  // For example:
  // playerName: string;
  // playerScore: number;
  // onChange: (value: string) => void;
}

